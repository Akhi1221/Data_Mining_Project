-train_projet3.py is used for training the model
-test_proect3.py is used for Testing the model
-For Testing model give the test file as input to train_projet3.py 
-Place the test file in the same folder as that of the code and input the testfile in the 
 test_proect3.py in place of "mealdata1.csv"(line 45)
-Outputs is matrix with 2 columns one w.r.t labels assigned using the Kmeans and other with DBSCAN